# Clean-environment Publication Smoke Test

CI must install a controlled browser renderer, assemble sample Master HTML, render PDF, run preflight, rasterize at least one page, and verify expected artifacts. CI success does not replace local visual inspection. Renderer metadata and page count are retained as artifacts.
