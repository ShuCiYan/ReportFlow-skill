# Knowledge Adapters

Local Markdown is the required baseline adapter. An Obsidian vault is an optional filesystem-compatible path; status must be explicitly recorded as `IMPLEMENTED`, `OPTIONAL`, `DOCUMENTED`, or `UNAVAILABLE`. Project facts stay under `knowledge/projects/<project-id>/`; only validated reusable methods, patterns, failure lessons and QA rules may enter `knowledge/shared/`.
