# Publication Gate

Pass only when: Master HTML assembled; HTML integrity checks pass; print CSS loaded; PDF rendered; rasterized pages inspected; defects fixed and re-rendered; pagination and TOC page numbers reconciled; citations, figures and sources unchanged. Record `PASS / REVISE / BLOCKED` with artifacts.
