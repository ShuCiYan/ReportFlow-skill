# Master HTML Assembly

Use `scripts/assemble_master.py --manifest <file> --out <html>`. The manifest declares title, subtitle, CSS, and ordered chapter files. The assembler inserts chapter bodies in order and emits a deterministic HTML file; it never renumbers citations or figures.
