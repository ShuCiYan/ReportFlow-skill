# Sprint 3C.1 Acceptance

Renderer inventory is recorded in the Sprint 3 test artifact. Run `setup_publication_env.py` without `--install` for detection-only mode. No global dependency installation is performed by default. CI reproducibility is defined in `.github/workflows/publication-smoke.yml`.
